"""
FastAPI application package.
# パッケージ化
__init__.py が存在することで、そのディレクトリ（ここでは app）をPythonが「パッケージ」として扱います。
これにより、import app.main のようなインポートが可能になります。

# 空でもOK
ファイルの中身が空でも構いません。
ただし、パッケージの初期化処理や共通の設定を書いても良いです。

# 現代Python（3.3以降）
厳密には「init.py」がなくてもサブディレクトリはパッケージとして認識されます（implicit namespace package）。
しかし、明示的にパッケージ化したい場合や、古いツール・環境との互換性を考えると、__init__.py を置くのがベストプラクティスです。
"""